# MinIO 上传模块 — 待讨论并修复

> 状态：待讨论 | 创建日期：2026-06-09

---

## 背景

在文档与项目一致性检查中发现，代码中存在 `backend/app/modules/minio_upload/` 模块（含 `router.py`, `schemas.py`），但架构设计文档的项目结构和模块列表中均未提及该模块。

## 模块当前状态

```
backend/app/modules/minio_upload/
├── __init__.py
├── router.py      # 获取上传 presigned URL
└── schemas.py     # 请求/响应模型
```

该模块提供 MinIO presigned URL 上传功能，用于前端直传文件（如宠物照片、检查报告等）。

## 待讨论事项

1. **该模块的应用场景需要明确：**
   - 寄养登记时上传宠物照片？
   - 就诊时上传检查报告（X 光、化验单）？
   - 客户档案中的宠物头像？
   - 其他场景？

2. **前端未对接：**
   - 目前前端没有任何页面调用 MinIO 上传接口
   - 需要确定哪些页面需要添加上传功能

3. **权限控制：**
   - 当前后端无角色限制（`get_current_user`）
   - 需要确定各角色对上传功能的权限边界

4. **文档补齐：**
   - 架构设计文档 Section 5（MinIO 文件存储）已有目录结构设计
   - Section 6（项目结构）需补充 `minio_upload` 模块
   - 前端设计文档需补充上传交互流程

## 建议行动

- [ ] 明确上传需求场景
- [ ] 确定前端对接页面
- [ ] 添加适当的角色权限控制
- [ ] 更新架构文档和前端设计文档
